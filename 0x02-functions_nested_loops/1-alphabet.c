#include <stdio.h>
#include "main.h"

/**
 * print_alphabet - Entry point
 * function - for
 * Return: 0 (Success)
 */

void print_alphabet(void)
{
	int = (alphabet[26]) "abcdefghijklmnopqrstuvwxyz";
	char i;

	for (i = 1; i <= 26; i++)
	{
		putchar(alphabet[i]);
	}
	putchar("\n");
	return (0);
}
