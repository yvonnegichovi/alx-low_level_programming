#include <stdio.h>
/**
 * main - entry point
 * funtion - sizeof
 * return: 0 (success)
 */
int main(void)
{
	int a;
	char b;
	long int c;
	float d;
	printf("Size of the variable is:%d,%c,%ld,%f bytes", sizeof(a, b, c, d);
	return (0);
}
